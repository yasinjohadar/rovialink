<div class="auth-header-actions">
    <button type="button" class="theme-toggle" aria-label="تبديل الوضع الداكن">
        <i class="fas fa-sun"></i>
    </button>
</div>

<div class="auth-mobile-logo">
    <a href="{{ route('frontend.home') }}">
        <i class="fas fa-store"></i>
        <span>إديو<span class="accent">ستور</span></span>
    </a>
</div>

<div class="auth-form-header">
    <h2>{{ $heading }}</h2>
    <p>{{ $subheading }}</p>
</div>
